# Alternative recipe shell (awk)
mk -n -f mkfile
cmp stdout expected

-- mkfile --
awktarget:Sawk -f /dev/stdin:
	BEGIN{print "greetings from awk"}
-- expected --
awktarget: BEGIN{print "greetings from awk"}
