# Prereq failure in parallel mode exercises the channel status path.
# Note: mk currently discards mkNodePrereqs return value, so the parent
# target still builds even though the prereq failed.
mk -p 2 -f mkfile
stdout 'dep: false'

-- mkfile --
all:V: dep
	echo building all
dep:V:
	false
